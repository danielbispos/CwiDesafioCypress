{
  "name": "desafiocypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "carrinho:test": "npx cypress run --spec='cypress/e2e/ecommerce/carrinho.cy.js'",
    "cy:open": "npx cypress open",
    "cy:run": "npx cypress run",
    "cy:open-dev": "npx cypress open --env dev=true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^13.0.2",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^10.10.0",
    "esbuild": "^0.15.12"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/support/step_definitions/**/*.{js,ts}"
  }
}